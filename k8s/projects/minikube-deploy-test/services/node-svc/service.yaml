apiVersion: v1
kind: Service
metadata:
  name: node-svc
  namespace: default
  labels:
    app: node-svc
    app.kubernetes.io/managed-by: grad-deploy
spec:
  type: ClusterIP
  selector:
    app: node-svc
  ports:
    - port: 3000
      targetPort: 3000